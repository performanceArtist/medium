import { create, subscribe, subscribeWith } from './source';
import { isSource, input, setFor } from './utils';
export * from './model';

export const source = {
  create,
  subscribe,
  subscribeWith,
  isSource,
  input,
  setFor,
};
