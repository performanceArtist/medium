import { create, subscribe } from './source';
import { isSource, input, setFor } from './utils';
export * from './model';

export const source = {
  create,
  subscribe,
  isSource,
  input,
  setFor,
};
